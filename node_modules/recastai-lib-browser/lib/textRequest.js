'use strict';

var _recastai = require('recastai');

var _recastai2 = _interopRequireDefault(_recastai);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

function textRequest(text, param) {
  return new Promise(function (resolve, reject) {
    if (!text) {
      reject('no message to send');
    } else {
      var client = new _recastai2.default.Client(param.userToken, param.language);
      client.textRequest(text, function (res, err) {
        if (err) {
          reject(err);
        } else {
          resolve(res);
        }
      });
    }
  });
}

module.exports = textRequest;